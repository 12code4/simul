{
  "name": "simul",
  "private": true,
  "version": "0.8.0",
  "type": "module",
  "description": "A game project (working title: simul).",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "typescript": "^5.6.0",
    "vite": "^5.4.0"
  }
}
